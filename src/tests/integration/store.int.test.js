describe("Success cases", () => {
  test("Should do something", () => {
    expect(1 + 1).toBe(2);
  })
})

describe("Errors cases", () => {
  test("Should do something", () => {
    expect(1 + 1).toBe(2);
  });
});